import React from 'react'

it ('renders without crashing', () => {

})

it ('matches the snapshot', () => {

})

it ('displays the usersname if the user is logged in', () => {

})

it ('routes to the new bookshelf route when clicking on add bookshelf', () => {

})

it ('displays the correct number of bookshelves', () => {

})

it ('routes to the bookshelf show route when user clicks on a bookshelf', () => {
  
})

